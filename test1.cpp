#include <cmath>
#include <cstdio>
#include <iostream>
using namespace std;

double cal(double end, double begin) {
    return (end - begin) / ((end + begin) / 2);
}

int main() {
double x = 2*acos(0);
    cout << x << endl;
}