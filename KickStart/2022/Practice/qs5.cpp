#include <cstdio>
using namespace std;

char Buffer[105];

struct Solution {
    int N, M, P;
    void Solve(int caseNo) {
        scanf("%d%d%d", &N, &M, &P);
        for (int i = 0; i < N; i++) {
            scanf("%s", Buffer);
            
        }
    }
};

int main() {
    int t;
    scanf("%d", &t);
    for (int i = 1; i <= t; i++) Solution().Solve(i);
    return 0;
}