#include <cstdio>
#include <string>
using namespace std;

const int mod = 1000000007;

struct Solution {
    int V, S;

    void Solve(int caseNo) {
        scanf("%d%d", &V, &S);

    }
};

int main() {
    int t;
    scanf("%d", &t);
    for (int i = 1; i <= t; i++) Solution().Solve(i);
    return 0;
}  

